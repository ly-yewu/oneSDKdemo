# oneSDKdemo

A simple sdk

#### Steps to Reproduce the Issue

1、npm install
2、npm run build

You can get the error:'Unknown word'that because postcss-import not working,which causes the @import syntax to be unrecognized in the demo.vue
